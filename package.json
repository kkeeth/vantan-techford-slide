{
  "private": true,
  "scripts": {
    "build": "slidev build && npm run authProvide",
    "dev": "slidev --open",
    "export": "slidev export",
    "authProvide": "rm -rf tmp && mkdir tmp && mv dist/* tmp/ && mkdir dist/public && mv tmp/* dist/public/ && cp app.js dist/app.js"
  },
  "dependencies": {
    "@slidev/cli": "^0.27.1",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*",
    "slidev-theme-penguin": "^1.1.2",
    "vite-svg-loader": "^2.2.0"
  },
  "name": "techford-javascript-slide"
}
